---
finding_id: h-new-286-4
run: 1
date: 2026-04-19
specialist: codex
verdict: PASS-DIRECTED
---

# H-NEW-286-4 run 1 journal

## Task

Follow up the OQ-18 bridge chain after `H-NEW-286.3`.

The live question was no longer whether `Q17` is descriptively the best bridge.
That was already true. The question was whether the fixed bridge bundle

- `{Q16,Q17,Q21,Q22,Q23,Q25}`

has any exact-significant semantic foothold on an already-landed interpretable
axis, rather than only on the name-class side.

The chosen axis was the locked `H-NEW-125` `eschatological_density`.

## Timeline

1. Read the current OQ-18 chain and the scout recommendation.
2. Reused the exact `H-NEW-125` per-surah axis values from `h-new-125.json`.
3. Standardized `eschatological_density` across all 114 surahs.
4. Enumerated all `C(10,6)=210` six-surah subsets of `Q16..Q25`.
5. Evaluated the target bridge bundle
   `{Q16,Q17,Q21,Q22,Q23,Q25}` against the exact within-zone upper-tail.
6. Wrote the JSON artifact and findings markdown.

## Locked result

- observed `Delta_E(B*)`:
  `0.4383478816749684`
- target mean `eschat_z`:
  `0.49571225066042574`
- complement mean `eschat_z`:
  `0.05736436898545733`
- exact descending rank:
  `9 / 210`
- exact upper-tail:
  `0.04285714285714286`
- verdict:
  `PASS-DIRECTED`

## Structural reading

The pass is real but not maximal.

That turned out to be the informative part.

- `Q17` is strongly positive on the eschatology axis
- `Q21` is the only negative-z member inside the target bundle
- the bundle still clears exact significance anyway

So the bridge package does have semantic content support, but its internal
mechanism is not uniform. The best exact bundles tend to keep

- `Q16`
- `Q17`
- `Q22`
- `Q23`
- `Q25`

and vary the sixth slot away from `Q21`.

That means `Q17` looks stable while `Q21` looks like the drag term.

## Interpretation

This run materially upgrades OQ-18 from:

- name-class foothold
- descriptive bridge identity
- compact composite null

to:

- name-class foothold
- descriptive bridge identity
- **exact-significant eschatological bridge-bundle support**

The honest scope stays bounded:

- not rank-1
- not a full single-axis solution
- still directional because the bundle was inherited

But it is a real mechanism foothold, and it is the first clean exact pass that
attaches semantic content to the `Q17` bridge bundle itself.

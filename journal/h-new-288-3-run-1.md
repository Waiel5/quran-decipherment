---
finding_id: h-new-288-3
run: 1
date: 2026-04-19
specialist: codex
verdict: NULL
---

# H-NEW-288.3 run 1 journal

## Task

Follow the OQ-19 frontier after `H-NEW-288.2`.

The post-`H-NEW-288.2` question was whether the exact `H-NEW-273` speech-act
score projects toward the fixed residualized short-core from `H-NEW-288.1`,
away from it, or neither.

This was the first whole-axis test after the local opener/refuge split landed.

## Timeline

1. Reused the exact `H-NEW-273` score on the fixed 5-7 verse Early-Meccan side
   `B`.
2. Reused the fixed residualized short-core `K = {Q108,Q106,Q103,Q112}` from
   `H-NEW-288.1`.
3. Computed residualized core-closeness on the four primary metrics.
4. Evaluated the observed mean correlation summary `T_proj`.
5. Enumerated the exact `336` unique score assignments for the null.
6. Wrote the JSON artifact and findings markdown.

## Locked result

- observed `T_proj`:
  `-0.48588450389600574`
- per-metric correlations:
  `-0.4831846585`, `-0.4830644500`, `-0.4886546302`, `-0.4886342769`
- exact `p_same`:
  `0.8902077151335311`
- exact `p_comp`:
  `0.11572700296735905`
- verdict:
  `NULL`

## Structural reading

The negative direction is consistent but not strong enough.

That turned out to be the informative part.

- `Q1` still sits far from the residualized short-core
- `Q113` and `Q114` are closer than `Q1`
- the score axis as a whole therefore trends away from the core
- but not strongly enough to certify a clean complementary-projection model

So the local `H-NEW-288.2` split is real, but it does not scale into a simple
ordered projection across all of `B`.

## Interpretation

This run removes the easiest next overclaim from OQ-19.

The project can no longer honestly say either:

- the H-NEW-273 speech-act axis is the same thing as the residualized short-core
- or that the speech-act axis cleanly runs opposite to that core

The right state after this run is:

- narrow foothold real
- local medoid mechanism real
- opener/refuge split real
- whole-axis projection still unresolved
